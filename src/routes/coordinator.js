export const homeButton = (navigate) => {
    navigate("/")
}

export const loginButton = (navigate) => {
    navigate(`/login`)
}
export const registerButton = (navigate) => {
    navigate(`/register`)
}
export const destinyButton = (navigate) => {
    navigate(`/destiny`)
}
export const contactButton = (navigate) => {
    navigate(`/contact`)
}
export const cartButton = (navigate) => {
    navigate(`/cart`)
}